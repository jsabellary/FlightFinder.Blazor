<div class="container-fluid py-2">
    <!-- Airport selection -->
    <div class="row mb-2">
        <!-- From airport -->
        <div class="col-md-5">
            <div class="input-group input-group-sm">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="bi bi-airplane-fill"></i>&nbsp;From</span>
                </div>
                <input list="airports" placeholder="Airport code..." class="form-control"
                    @bind="criteria.FromAirport" />
            </div>
        </div>
        <div class="col-md-1 text-center align-self-center">➝</div>

        <!-- To airport -->
        <div class="col-md-5">
            <div class="input-group input-group-sm">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="bi bi-airplane-fill"></i>&nbsp;To</span>
                </div>
                <input list="airports" placeholder="Airport code..." class="form-control" @bind="criteria.ToAirport" />
            </div>
        </div>
    </div>

    <!-- Departure / return dates -->
    <div class="row mb-2">
        <!-- Depart date -->
        <div class="col-md-5">
            <div class="input-group input-group-sm">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="bi bi-calendar-event"></i>&nbsp;Depart</span>
                </div>
                <input type="date" @bind="criteria.OutboundDate" @bind:format="yyyy-MM-dd" class="form-control" />
            </div>
        </div>
        <div class="col-md-1 text-center align-self-center">➝</div>

        <!-- Return date -->
        <div class="col-md-5">
            <div class="input-group input-group-sm">
                <div class="input-group-prepend">
                    <span class="input-group-text"><i class="bi bi-calendar-event"></i>&nbsp;Return</span>
                </div>
                <input type="date" @bind="criteria.ReturnDate" @bind:format="yyyy-MM-dd" class="form-control" />
            </div>
        </div>
    </div>

    <!-- Class / search -->
    <div class="row mb-2">
        <div class="col-md-6">
            <select class="custom-select custom-select-sm" @bind="criteria.TicketClass">
                <option value=@TicketClass.Economy>Economy</option>
                <option value=@TicketClass.PremiumEconomy>Premium Economy</option>
                <option value=@TicketClass.Business>Business</option>
                <option value=@TicketClass.First>First</option>
            </select>
        </div>
        <div class="col-md-6 text-md-right">
            <button @onclick="() => OnSearch.InvokeAsync(criteria)" type="button"
                class="btn btn-light btn-sm btn-block font-weight-bold">
                Search ➝
            </button>
        </div>
    </div>
</div>
<AirportsList />

@code
{
    [Parameter]
    public EventCallback<SearchCriteria> OnSearch { get; set; }

    private SearchCriteria criteria = new SearchCriteria("LHR", "SEA");
}
